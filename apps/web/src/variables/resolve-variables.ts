// Re-export from @minopencil/pen-core — the canonical source
export {
  isVariableRef,
  getDefaultTheme,
  resolveVariableRef,
  resolveColorRef,
  resolveNumericRef,
  resolveNodeForCanvas,
} from '@minopencil/pen-core';
